# Config package — typed, git-versioned agent configuration (prompts/playbooks/thresholds)
# plus the version + kb_version stamps that every episode and session carry.
from .settings import AgentConfig, load_config  # noqa: F401
