# Copy to `.env.local` and adjust. Only NEXT_PUBLIC_* vars reach the browser.
#
# Base URL of the FastAPI backend (consent, chat, LiveKit token endpoints).
# The demo console calls `${NEXT_PUBLIC_API_BASE}/api/...`.
NEXT_PUBLIC_API_BASE=http://localhost:8000
